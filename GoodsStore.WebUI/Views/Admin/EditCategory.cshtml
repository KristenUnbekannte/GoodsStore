@model GoodsStore.BusinessLogic.Models.CategoryModel

@{
    ViewBag.Title = "Админ панель: редактирование категории";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="admin_body_small table-bordered">
    <div class="panel">
        <div class="panel-heading">
            <h3>Редактирование категории «@Model.Name»</h3>
        </div>

        @using (Html.BeginForm("EditCategory", "Admin",
         FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <div class="panel-body">
                @Html.HiddenFor(m => m.CategoryId)
                @Html.Label("Название")
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Name)
            </div>
            <div class="panel-footer">
                <input type="submit" value="Сохранить" class="btn btn-success" />
                @Html.ActionLink("Отменить изменения и вернуться к списку", "ListCategories", null, new
            {
                @class = "btn btn-primary"
            })
            </div>
        }
    </div>
</div>