@model IEnumerable<GoodsStore.BusinessLogic.Models.ClothesTypeModel>

@{
    ViewBag.Title = "Админ панель: типы одежды";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="admin_body_small">
    <div class="panel panel-primary">
        <div class=" panel-heading">
            <h4>Типы одежды</h4>
        </div>
        <table class="table table-striped table-condensed">
            <tr>
                <th class="text-center">ID</th>
                <th>Название</th>
                <th class="text-center" colspan="2">Действие</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-center">@item.ClothesTypeId.</td>
                    <td>@item.Name</td>
                    <td class="text-right">
                        @using (Html.BeginForm("EditClothesType", "Admin", FormMethod.Get))
                        {
                            @Html.Hidden("ClothesTypeId", item.ClothesTypeId)
                            <button type="submit" class="btn btn-success btn-xs">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </button>
                        }
                    </td>
                    <td class="text-left">
                        @using (Html.BeginForm("DeleteClothesType", "Admin"))
                        {
                            @Html.Hidden("ClothesTypeId", item.ClothesTypeId)
                            <button type="submit" class="btn btn-warning btn-xs">
                                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                            </button>
                        }
                    </td>
                </tr>
            }
        </table>
        <div class="panel-footer">
            @Html.ActionLink("Добавить тип одежды", "AddClothesType", null,
            new { @class = "btn btn-success" })
        </div>
    </div>
</div>