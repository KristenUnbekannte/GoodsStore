@model IEnumerable<GoodsStore.BusinessLogic.Models.SizeModel>

@{
    ViewBag.Title = "Админ панель: список размеров";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="admin_body_small">
    <div class="panel panel-primary">
        <div class=" panel-heading">
            <h4>Список размеров</h4>
        </div>
        <table class="table table-striped table-condensed">
            <tr>
                <th class="text-center">ID</th>
                <th>Название</th>
                <th class="text-center" colspan="2">Действие</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-center">@item.SizeId.</td>
                    <td>@item.Name</td>
                    <td class="text-right">
                        @using (Html.BeginForm("EditSize", "Admin", FormMethod.Get))
                        {
                            @Html.Hidden("sizeId", item.SizeId)
                            <button type="submit" class="btn btn-success btn-xs">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </button>
                        }
                    </td>
                    <td class="text-left">
                        @using (Html.BeginForm("DeleteSize", "Admin"))
                        {
                            @Html.Hidden("SizeId", item.SizeId)
                            <button type="submit" class="btn btn-warning btn-xs">
                                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                            </button>
                        }
                    </td>
                </tr>
            }
        </table>
        <div class="panel-footer">
            @Html.ActionLink("Добавить размер", "AddSize", null,
            new { @class = "btn btn-success" })
        </div>
    </div>
</div>